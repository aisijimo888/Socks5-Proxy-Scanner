name: 代理池自动更新并同步�?Cloudflare KV

on:
  schedule:
    # �?6 小时运行一�?(00:00, 06:00, 12:00, 18:00 UTC)
    - cron: '0 */6 * * *'
  workflow_dispatch:  # 允许手动触发
  push:
    branches:
      - main
    paths-ignore:
      - '**.md'
      - 'docs/**'

# GitHub Secrets 配置说明�?
# 需要在仓库 Settings -> Secrets and variables -> Actions 中配置以下变量：
#
# 必需变量�?
# - CLOUDFLARE_API_TOKEN: Cloudflare API Token (需�?Workers KV:Edit 权限)
# - CLOUDFLARE_ACCOUNT_ID: Cloudflare 账户 ID
#
# 可选变量（如果需�?Telegram 通知）：
# - TELEGRAM_BOT_TOKEN: Telegram Bot Token
# - TELEGRAM_CHAT_ID: Telegram Chat ID

jobs:
  update-proxies:
    runs-on: ubuntu-latest
    
    steps:
      # 1. 检出代�?
      - name: 检出代�?
        uses: actions/checkout@v4
      
      # 2. 设置 Python 环境
      - name: 设置 Python 环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      # 3. 安装依赖
      - name: 安装依赖
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
      
      # 4. 运行代理扫描�?
      - name: 运行代理扫描�?
        env:
          DATABASE_PATH: proxies.db
          SCAN_TIMEOUT: 10
          MAX_CONCURRENCY: 150
        run: |
          echo "🔍 开始扫描代�?.."
          python proxy_scanner_enhanced.py \
            --timeout 10 \
            --max-concurrency 150 \
            --enable-blacklist \
            --auto-blacklist \
            --blacklist-threshold 1 \
            --cleanup-days 7
          
          echo "📊 检查生成的文件..."
          ls -lh proxies.json || echo "⚠️ proxies.json 未生�?
          
          # 显示代理数量
          if [ -f proxies.json ]; then
            PROXY_COUNT=$(python -c "import json; data=json.load(open('proxies.json')); print(data['metadata']['total'])")
            echo "�?成功获取 ${PROXY_COUNT} 个有效代�?
          fi
      
      # 5. 上传�?Cloudflare KV
      - name: 上传�?Cloudflare KV
        if: success()
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          echo "📤 上传 proxies.json �?Cloudflare KV..."
          
          # 检查文件是否存�?
          if [ ! -f proxies.json ]; then
            echo "�?proxies.json 不存在，跳过上传"
            exit 1
          fi
          
          # 读取 JSON 内容
          PROXY_DATA=$(cat proxies.json)
          
          # 使用 Cloudflare API 上传�?KV
          # 假设您的 KV namespace ID �?PROXY_POOL_ID (需要在仓库 Secrets 中配�?
          KV_NAMESPACE_ID="${{ secrets.KV_NAMESPACE_ID }}"
          
          curl -X PUT "https://api.cloudflare.com/client/v4/accounts/${CLOUDFLARE_ACCOUNT_ID}/storage/kv/namespaces/${KV_NAMESPACE_ID}/values/current_proxies" \
            -H "Authorization: Bearer ${CLOUDFLARE_API_TOKEN}" \
            -H "Content-Type: application/json" \
            --data-binary @proxies.json
          
          echo "�?已上传到 Cloudflare KV (键名: current_proxies)"
      
      # 6. 备份�?GitHub Artifacts
      - name: 备份�?Artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: proxy-results-${{ github.run_number }}
          path: |
            proxies.json
            proxies.db
            scanner.log
          retention-days: 7
          if-no-files-found: warn
      
      # 7. 发送成功通知 (可�?
      - name: 发�?Telegram 成功通知
        if: success() && vars.TELEGRAM_BOT_TOKEN != ''
        run: |
          if [ -n "${{ secrets.TELEGRAM_BOT_TOKEN }}" ] && [ -n "${{ secrets.TELEGRAM_CHAT_ID }}" ]; then
            PROXY_COUNT=$(python -c "import json; data=json.load(open('proxies.json')); print(data['metadata']['total'])" 2>/dev/null || echo "0")
            UPDATE_TIME=$(python -c "import json; data=json.load(open('proxies.json')); print(data['metadata']['updated_at'])" 2>/dev/null || echo "Unknown")
            
            MESSAGE="�?*代理池更新成�?

📊 统计信息:
- 总代理数: ${PROXY_COUNT}
- 更新时间: ${UPDATE_TIME}
- 运行编号: #${{ github.run_number }}

📦 已同步到 Cloudflare KV
🔗 仓库: https://github.com/${{ github.repository }}"

            curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
              -d "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" \
              -d "text=${MESSAGE}" \
              -d "parse_mode=Markdown" \
              -d "disable_web_page_preview=true"
            
            echo "�?Telegram 通知已发�?
          else
            echo "ℹ️ 跳过 Telegram 通知 (未配�?"
          fi
      
      # 8. 发送失败通知 (可�?
      - name: 发�?Telegram 失败通知
        if: failure() && vars.TELEGRAM_BOT_TOKEN != ''
        run: |
          if [ -n "${{ secrets.TELEGRAM_BOT_TOKEN }}" ] && [ -n "${{ secrets.TELEGRAM_CHAT_ID }}" ]; then
            MESSAGE="�?*代理池更新失�?

�?时间: $(date +'%Y-%m-%d %H:%M:%S UTC')
🔗 查看日志: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"

            curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
              -d "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" \
              -d "text=${MESSAGE}" \
              -d "parse_mode=Markdown"
            
            echo "�?Telegram 失败通知已发�?
          fi
      
      # 9. 生成工作流摘�?
      - name: 生成工作流摘�?
        if: always()
        run: |
          echo "# 📊 代理池更新摘�? >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ -f proxies.json ]; then
            PROXY_COUNT=$(python -c "import json; data=json.load(open('proxies.json')); print(data['metadata']['total'])" 2>/dev/null || echo "0")
            UPDATE_TIME=$(python -c "import json; data=json.load(open('proxies.json')); print(data['metadata']['updated_at'])" 2>/dev/null || echo "Unknown")
            
            echo "## �?执行成功" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "| 指标 | 数�?|" >> $GITHUB_STEP_SUMMARY
            echo "|------|------|" >> $GITHUB_STEP_SUMMARY
            echo "| 总代理数 | ${PROXY_COUNT} |" >> $GITHUB_STEP_SUMMARY
            echo "| 更新时间 | ${UPDATE_TIME} |" >> $GITHUB_STEP_SUMMARY
            echo "| 运行编号 | #${{ github.run_number }} |" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "�?已同步到 Cloudflare KV (键名: current_proxies)" >> $GITHUB_STEP_SUMMARY
          else
            echo "## �?执行失败" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "proxies.json 文件未生成，请查看日志�? >> $GITHUB_STEP_SUMMARY
          fi
