name: 自动更新代理池

on:
  schedule:
    # 每天运行两次 (00:00 UTC, 12:00 UTC)
    - cron: '0 0,12 * * *'
  workflow_dispatch:  # 允许手动触发

permissions:
  contents: write  # 允许写入仓库内容
  actions: read    # 允许读取Actions状态

jobs:
  update-proxies:
    runs-on: ubuntu-latest
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v3
      
      - name: 设置Python环境
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: 缓存pip依赖
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-
      
      - name: 安装依赖
        run: |
          pip install -r requirements.txt
      
      - name: 运行代理扫描器
        env:
          DATABASE_PATH: proxies.db
          SCAN_TIMEOUT: 8
          MAX_CONCURRENCY: 100  # 提升到100（适应Actions环境）
        run: |
          # 创建subscribe目录（确保存在且有写入权限）
          mkdir -p subscribe
          chmod 755 subscribe
          # 运行主程序，启用黑名单功能（一次失败即拉黑，7天清理）
          python proxy_scanner_enhanced.py --timeout 8 --max-concurrency 100 --output subscribe/proxies.json --enable-blacklist --auto-blacklist --blacklist-threshold 1 --cleanup-days 7
      
      - name: 生成订阅文件
        run: |
          echo "📦 生成订阅文件..."
          # 确保subscribe目录存在
          ls -la subscribe/
          python subscription_generator.py
          echo "✅ 订阅文件生成完成"
          # 显示生成的文件
          ls -la subscribe/
      
      - name: 生成统计摘要
        run: |
          python -c "
          import json
          import os
          from datetime import datetime
          
          try:
              # 从 subscribe 目录读取
              if os.path.exists('subscribe/proxies.json'):
                  with open('subscribe/proxies.json', 'r', encoding='utf-8') as f:
                      data = json.load(f)
                      # 处理新格式：包含metadata的包装结构
                      if isinstance(data, dict) and 'proxies' in data:
                          proxies = data['proxies']
                      elif isinstance(data, list):
                          proxies = data
                      else:
                          proxies = []
              else:
                  proxies = []
              
              total = len(proxies)
              # 简单的国家统计
              countries = {}
              for p in proxies:
                  c = p.get('country', 'Unknown')
                  countries[c] = countries.get(c, 0) + 1
              
              # 生成 Markdown 摘要
              summary = f'''
              # 📊 代理池更新摘要
              
              **更新时间:** {datetime.now().strftime('%Y-%m-%d %H:%M:%S UTC')}
              
              ## 📈 统计信息
              
              | 指标 | 数值 |
              |------|------|
              | ✅ 总代理数 | {total} |
              | 🌍 国家数量 | {len(countries)} |
              
              ## 🌍 国家分布 (Top 10)
              
              '''
              
              # 添加国家分布
              if countries:
                  sorted_countries = sorted(countries.items(), key=lambda x: x[1], reverse=True)[:10]
                  summary += '| 国家 | 数量 |\\n|------|------|\\n'
                  for country, count in sorted_countries:
                      summary += f'| {country} | {count} |\\n'
              else:
                  summary += '*暂无数据*\\n'
              
              summary += '''
              
              ## 📁 订阅链接
              
              点击下方链接获取最新代理订阅：
              
              - 🔷 [Clash 订阅](https://raw.githubusercontent.com/${{ github.repository }}/main/subscribe/clash.yaml)
              - 🔶 [V2Ray 订阅](https://raw.githubusercontent.com/${{ github.repository }}/main/subscribe/v2ray.json)
              - 📄 [纯文本列表](https://raw.githubusercontent.com/${{ github.repository }}/main/subscribe/proxies.txt)
              '''
              
              # 写入到 GitHub Step Summary
              with open(os.environ['GITHUB_STEP_SUMMARY'], 'w', encoding='utf-8') as f:
                  f.write(summary)
              
              print('✅ Actions 摘要已生成')
          except Exception as e:
              print(f'❌ 生成摘要失败: {e}')
          "
      
      - name: 提交更新
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          
          # 显示subscribe目录内容
          echo "📂 subscribe目录文件列表:"
          ls -lh subscribe/
          
          # 直接添加整个subscribe目录和数据库文件
          git add -A subscribe/
          git add -f proxies.db
          
          # 显示将要提交的文件
          echo ""
          echo "📋 将要提交的文件:"
          git diff --staged --name-only
          
          # 检查是否有文件被staged
          if git diff --staged --quiet; then
            echo "⚠️ 没有文件被staged，跳过提交"
            exit 0
          fi
          
          # 提交
          git commit -m "🔄 自动更新代理池 $(date +'%Y-%m-%d %H:%M:%S')"
          
          # 拉取并推送
          git pull --rebase origin main || true
          git push
          echo "✅ 文件已成功推送到GitHub"

      - name: 上传构建产物
        uses: actions/upload-artifact@v4
        with:
          name: proxy-results
          path: |
            subscribe/
            proxies.db
          retention-days: 5

      - name: 发送Telegram成功通知
        if: success()
        run: |
          if [ -n "${{ secrets.TELEGRAM_BOT_TOKEN }}" ] && [ -n "${{ secrets.TELEGRAM_CHAT_ID }}" ]; then
            # 读取代理统计
            TOTAL_PROXIES=$(grep -o '"total_proxies": [0-9]*' subscribe/proxies.json | grep -o '[0-9]*' | head -1 || echo "0")
            
            MESSAGE="✅ *代理池更新成功*

📊 统计信息:
- 总代理数: ${TOTAL_PROXIES}
- 更新时间: $(date +'%Y-%m-%d %H:%M:%S')

📁 生成的文件:
$(ls -lh subscribe/ | tail -n +2 | awk '{print "- " $9 " (" $5 ")"}'  || echo "- 文件列表获取失败")

🔗 仓库: https://github.com/${{ github.repository }}"

            curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
              -d "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" \
              -d "text=${MESSAGE}" \
              -d "parse_mode=Markdown" \
              -d "disable_web_page_preview=true"
            
            echo "✅ Telegram通知已发送"
          else
            echo "⚠️ 跳过Telegram通知: 未配置TELEGRAM_BOT_TOKEN或TELEGRAM_CHAT_ID"
          fi

      - name: 发送Telegram失败通知
        if: failure()
        run: |
          if [ -n "${{ secrets.TELEGRAM_BOT_TOKEN }}" ] && [ -n "${{ secrets.TELEGRAM_CHAT_ID }}" ]; then
            MESSAGE="❌ *代理池更新失败*

⏰ 时间: $(date +'%Y-%m-%d %H:%M:%S')
🔗 查看日志: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"

            curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
              -d "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" \
              -d "text=${MESSAGE}" \
              -d "parse_mode=Markdown"
            
            echo "✅ Telegram失败通知已发送"
          fi

