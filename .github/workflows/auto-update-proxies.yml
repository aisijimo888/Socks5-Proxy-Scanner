name: 自动更新代理池

on:
  schedule:
    # 每6小时运行一次
    - cron: '0 */6 * * *'
  workflow_dispatch:  # 允许手动触发

permissions:
  contents: write

jobs:
  update-proxies:
    runs-on: ubuntu-latest
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v3
      
      - name: 设置Python环境
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: 缓存pip依赖
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-
      
      - name: 安装依赖
        run: |
          pip install -r requirements.txt
      
      - name: 运行代理扫描器
        run: |
          # 运行主程序，它会生成 proxies.json 和 proxy_history.json
          python proxy_scanner_final.py
      
      - name: 生成订阅文件
        run: |
          echo "📦 生成订阅文件..."
          # 确保 subscribe 目录存在
          mkdir -p subscribe
          python subscription_generator.py
          echo "✅ 订阅文件生成完成"
      
      - name: 生成统计摘要
        run: |
          python -c "
          import json
          import os
          from datetime import datetime
          
          try:
              if os.path.exists('proxies.json'):
                  with open('proxies.json', 'r', encoding='utf-8') as f:
                      proxies = json.load(f)
              else:
                  proxies = []
              
              total = len(proxies)
              # 简单的国家统计
              countries = {}
              for p in proxies:
                  c = p.get('country', 'Unknown')
                  countries[c] = countries.get(c, 0) + 1
              
              # 生成 Markdown 摘要
              summary = f'''
              # 📊 代理池更新摘要
              
              **更新时间:** {datetime.now().strftime('%Y-%m-%d %H:%M:%S UTC')}
              
              ## 📈 统计信息
              
              | 指标 | 数值 |
              |------|------|
              | ✅ 总代理数 | {total} |
              | 🌍 国家数量 | {len(countries)} |
              
              ## 🌍 国家分布 (Top 10)
              
              '''
              
              # 添加国家分布
              if countries:
                  sorted_countries = sorted(countries.items(), key=lambda x: x[1], reverse=True)[:10]
                  summary += '| 国家 | 数量 |\\n|------|------|\\n'
                  for country, count in sorted_countries:
                      summary += f'| {country} | {count} |\\n'
              else:
                  summary += '*暂无数据*\\n'
              
              summary += '''
              
              ## 📁 订阅链接
              
              点击下方链接获取最新代理订阅：
              
              - 🔷 [Clash 订阅](https://raw.githubusercontent.com/${{ github.repository }}/main/subscribe/clash.yaml)
              - 🔶 [V2Ray 订阅](https://raw.githubusercontent.com/${{ github.repository }}/main/subscribe/v2ray.json)
              - 📄 [纯文本列表](https://raw.githubusercontent.com/${{ github.repository }}/main/subscribe/proxies.txt)
              '''
              
              # 写入到 GitHub Step Summary
              with open(os.environ['GITHUB_STEP_SUMMARY'], 'w', encoding='utf-8') as f:
                  f.write(summary)
              
              print('✅ Actions 摘要已生成')
          except Exception as e:
              print(f'❌ 生成摘要失败: {e}')
          "
      
      - name: 提交更新
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          # 添加所有生成的文件（都在subscribe目录）
          git add subscribe/ proxies.db
          # 只有在有变化时才提交
          git diff --quiet && git diff --staged --quiet || git commit -m "🔄 自动更新代理池 $(date +'%Y-%m-%d %H:%M:%S')"
          git push
